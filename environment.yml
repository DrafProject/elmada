name: elmada
channels:
  - conda-forge
  - anaconda
dependencies:
# NOTE: here '=' (single)
  - appdirs
  - bs4
  - fastparquet
  - ipython
  - lxml
  - matplotlib
  - numpy
  - pandas
  - pip
  - pyarrow
  - pytables
  - python>=3.7
  - python-snappy
  - quandl
  - requests
  - scipy
  - seaborn
  - xlrd
  # - notebook
  - pip:
  # NOTE: here '==' (double)
    - entsoe-py==0.2
    - '--editable=.[all]' # installs an editable version of elmada including feature dependencies
