name: elmada
channels:
  - conda-forge
  - anaconda
dependencies:  # NOTE: here '=' (single)
  - appdirs
  - bs4
  - ipython
  - lxml
  - matplotlib
  - numpy
  - pandas
  - pip
  - plotly
  - pyarrow
  - pytest
  - pytest-cov
  - pytest-mock
  - python>=3.7
  - quandl
  - requests
  - scipy
  - seaborn
  - xlrd
  # - notebook
  - pip:  # NOTE: here '==' (double)
    - entsoe-py==0.2.10
    - '--editable=.' # installs an editable version of elmada